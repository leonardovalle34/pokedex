import styled from "styled-components";

export const Header = styled.div`
  position: absolute;
  top: 10px;
`;

export const Pagination = styled.p`
margin-top: 0px;
position: relative;
top: -17px;
`
